services:
  web:
    image: tellebma/how_to_use_claude:latest
    build:
      context: .
      dockerfile: Dockerfile
    container_name: claude-codex
    ports:
      - "8080:8080"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/health"]
      interval: 30s
      timeout: 3s
      start_period: 5s
      retries: 3
    read_only: true
    tmpfs:
      - /tmp
      - /var/cache/nginx
      - /var/log/nginx
    security_opt:
      - no-new-privileges:true
